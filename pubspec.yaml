name: crdt_sync
description: A dart-native turnkey solution for painless network synchronization
version: 6.0.0
repository: https://github.com/cachapa/crdt_sync

environment:
  sdk: ^3.10.0

dependencies:
  crdt: #^6.0.0
#    path: ../crdt
    git:
      url: https://github.com/cachapa/crdt.git
      ref: v6
  web_socket_channel: ^3.0.3

dev_dependencies:
  lints: any
  test: any
  uuid: any
